# 06. Gradient Descent Algorithm

> **Core Concept**: **Gradient Descent** is an optimization algorithm used to automatically find the parameter values ($w, b$) that minimize a cost function $J(w,b)$. It is one of the most fundamental algorithms in modern Artificial Intelligence.

---

*(Ready for Gradient Descent content)*
